__pycache__/
*.pyc
.venv/
venv/
data/*.csv
data/*.json
# Hand-maintained input (not fetched, not regenerable) — keep it in the repo.
!data/reported_revpar.csv
st.log
.DS_Store
.streamlit/secrets.toml

# Generated analysis artifacts (reproducible via `python -m src.pipeline`)
outputs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.env

# Files the Claude desktop app drops into the folder
Claude outputs/
